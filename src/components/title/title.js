'use strict';
{
  // function handler(event){
  //   const titlesDOM = this.querySelector(`.title__text`);
  //   if (!titlesDOM) return;
  //   let titles = Number(this.dataset.count);
  //   if (!isFinite(titles)) titles = 0;
  //   titles += this.querySelector(`.title__input`) && this.querySelector(`.title__input`).checked ? 1 : 0;
  //   titlesDOM.textContent = `${titles}`;
  // }

  // const elements = document.querySelectorAll(`.title`);
  // elements.forEach((_, i, a) => {
  //   // click change
  //   a[i].addEventListener('change', handler, { passive: true, });
  // });
}